{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Prisma migrate dev (db)",
			"type": "shell",
			"command": "pnpm --filter @workspace/db exec prisma migrate dev --name add_eventsession_grouping",
			"problemMatcher": [
				"$eslint-stylish"
			]
		}
	]
}